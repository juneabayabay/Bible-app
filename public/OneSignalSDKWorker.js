/**
 * Optional OneSignal service worker (used when PUBLIC_ONESIGNAL_APP_ID is set).
 * Keep this file so OneSignal can register alongside the app shell worker.
 */
importScripts("https://cdn.onesignal.com/sdks/web/v16/OneSignalSDK.sw.js");
