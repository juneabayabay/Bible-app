# Supabase (Prayer wall — shared)
# Run supabase/prayer-wall.sql in the SQL editor first.
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key

# OneSignal (optional — broader push when tab is closed)
# Create a Web app at https://onesignal.com and paste the App ID.
# PUBLIC_ONESIGNAL_APP_ID=
